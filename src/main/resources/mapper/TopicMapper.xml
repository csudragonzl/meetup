<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.tjubd.meetup.mapper.TopicMapper">
    <select id="getGroupIdList" resultType="String">
        select group_id_list from `topics` where name = #{topicName}
    </select>
</mapper>